<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finn.mapper.MenuMapper">
    <select id="getMenuList" resultType="com.finn.entity.Menu">
        select
        m.id,
        m.menu_name,
        m.menu_url,
        m.parent_id,
        m.menu_sort,
        m.description,
        m.menu_icon
        from tb_menu m, tb_role_menu rm, tb_role r
        where rm.role_id = r.id
        and r.role_name = #{roleName}
        and rm.menu_id = m.id
    </select>
</mapper>
