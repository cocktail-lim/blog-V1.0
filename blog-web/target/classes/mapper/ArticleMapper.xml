<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finn.mapper.ArticleMapper">
    <resultMap id="getArticleListPageMap" type="com.finn.dto.ArticleListPageDTO">
        <id column="article_id" property="articleId"/>
        <result column="article_title" property="articleTitle"/>
        <result column="category_id" property="categoryId"/>
        <result column="is_top" property="isTop"/>
        <result column="is_draft" property="isDraft"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <collection property="tagDTOList" ofType="com.finn.dto.TagDTO">
            <id column="tag_id" property="tagId"/>
            <result column="tag_name" property="tagName"/>
        </collection>
    </resultMap>

    <select id="getArticleListPage" resultMap="getArticleListPageMap">
        SELECT
            a.article_id,
            a.article_title,
            a.category_id,
            a.is_top,
            a.is_draft,
            a.create_time,
            a.update_time,
            t.tag_id,
            t.tag_name
        FROM
            (
                SELECT
                    article_id,
                    article_title,
                    is_top,
                    is_draft,
                    create_time,
                    update_time,
                    category_id
                FROM
                    tb_article
                ORDER BY is_top DESC, article_id DESC
            ) AS a
                LEFT JOIN tb_category c ON a.category_id = c.category_id
                LEFT JOIN tb_article_tag atg ON a.article_id = atg.article_id
                LEFT JOIN tb_tag t ON t.tag_id = atg.tag_id
        ORDER BY is_top DESC, a.article_id DESC
    </select>
</mapper>
<!--    <insert id="createArticle">-->
<!--        insert-->
<!--        into tb_article a-->
<!--        (article_title, article_content, article_cover, category_id, is_top, create_time, update_time)-->
<!--        values(-->
<!--        #{ArticleUpdateVO.articleTitle},-->
<!--        #{ArticleUpdateVO.articleContent},-->
<!--        #{ArticleUpdateVO.articleCover},-->
<!--        #{ArticleUpdateVO.categoryId},-->
<!--        #{ArticleUpdateVO.isTop},-->
<!--        now(),-->
<!--        now()-->
<!--        )-->
<!--    </insert>-->
